
# Rust / Cargo
/target/
# Library crates do not commit Cargo.lock (reproducibility is the consumer's job)
Cargo.lock

# Editor / OS
.DS_Store
.idea/
*.rs.bk

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Internal review / audit tracking logs (not for distribution)
/audit/
